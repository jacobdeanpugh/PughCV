apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pughcv
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
# The Secret is created out-of-band from pughcv/.env.local; see README.md.
